export const metadata = {
  title: "Lg Service Center",
  description: "We repair your home with care and precision.",
};

const LgLayout = ({ children }) => {
  return children;
};

export default LgLayout;
