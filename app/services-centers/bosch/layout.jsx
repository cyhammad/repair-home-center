export const metadata = {
  title: "Bosch Service Center",
  description: "We repair your home with care and precision.",
};

const BoschLayout = ({ children }) => {
  return children;
};

export default BoschLayout;
